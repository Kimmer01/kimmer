using System;
using System.IO;

namespace DotnetStandardPractice
{
    public class FileStreamPractice
    {
        public Type GetType()
        {
            return typeof(FileStream);
        }
    }
}
